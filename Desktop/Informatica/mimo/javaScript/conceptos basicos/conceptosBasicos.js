// Ejercicio 1
const job = 'Astronauta';


// Ejercicio 2

const feeddback = 'Success!';
const response = feeddback + ' ' + 'Ticket booked';
console.log(response);

// Actualizacion de variables

let language = 'English';
language = 'Spanish';

// Crear una variable que se pueda cambiar con el nombre 'status' 
//y se use 'defaultStatus' para establecer su valor
// de mono que ambas variables tengan el mismo valor

const defaultStatus = 'Hi there!';
let status = defaultStatus;

// Crear una variable que no cambie con el nombre buttonLabel y 
//configurar en un valor para que el codigo muestre Go en consola

const startLabel = 'Go';
const stopLabel = 'Stop';
const buttonLabel = startLabel;

console.log(buttonLabel);

// Escribir expresion usando Variables

const wonder = 'Wonder';
const woman = 'Woman';

console.log(wonder + ' ' + woman);

// reasignar variable barista para que Louise cubra el turno

let barista = 'Brandon';
barista = 'Louise';

console.log("Today's shift: " + barista);

// crear bariable que no pueda cambiar result con nombre y 
//configurar en una expresion word1 y word2

const word1 = 'up';
const word2 = 'date';

const result = word1 + word2;

// tercer ejercicio 
// Crear variable con valor true y mostrae en consola

let valor = true;

console.log(valor);

// crear variable correct y almacenar el valor true 

let correct = true;

// crear una variable wrong y almacenar el valor false

let wrong = false; 

//crear variable con valor false y mostrar en conola

let error = false;

console.log(error);

// Crear una variable que almacene true y otra variable que 
// almacene false mostrar cada uno en consola

let verdad = true;
let falso = false;

console.log(verdad);
console.log(falso);

// Crear variable visible, almcene true y terminanar con un punto y como

let visible = true;

// Utilizar el operador de negacion con un valor booleano para generar 
// false en el registro final de la consola

const divide = 10 / 3;
console.log(divide);
console.log('Evenly divided:');
console.log(!true);

// negar el valor false

const allowCookies = !false;

// Crear una variable isAvailable y guardar el valor negado de openSlot dentro de ella

const openSlot = true;

const isAvailable = !openSlot; 

// utilizar negacion para mostrar false en consola

console.log(!true);

// Cuarto Ejercicios

// resultado de la comparacion sea true

console.log(5 === 5);

// Escribir una exprecion dentro de console.log() usando las dos 
// variables para ver si las variables np son iguales

const number1 = 17;
const number2 = 23;

console.log('diferent numbers: ');
console.log(number1 !== number2);

// Escribir una exprecion dentro de console.log() para verificar si today es igual a 13

const today = 12;
console.log(today === 13);

// ultilizar un operador para comprobar si points son iguales a 0 y 
// almace el resultado en la constante noPoints

const points = 142;
const noPoints = points === 0;

console.log(noPoints)

// Utilizar dos variables para ver si las puntuaciones son las mismas y 
// almacenar el rasultado en la constante
// sameScore para que se muestre en console.lo()

const player1 = 10;
const player2 = 10;
const sameScore = player1 === player2;

console.log(sameScore);

// Dentro de console.log() escribir una expresion para verificar si triesLeft no es igual a 0

const triesLeft = 1;
console.log(triesLeft !== 0);

// crear una instruccion console.log y comprobar si 0 no es igual a 1

console.log(0 !== 1);

// crear una variable sameAge. comparar si age1 es igual a age2 y almacenar el resultado en sameAge

const age1 = 7;
const age2 = 9;

const sameAge = age1 === age2;

// dentro de console.log() verificar si volumen es igual a 11

const volume = 11;

console.log(volume === 11);

// codificar para que se muestre true en consola

console.log(50 !== 500);

// crear una constante isWinner comparar si ChosenNumber es igual a 37 
// y almacenar el resultado en la constante isWinner

const chosenNumber = 20;
const isWinner = chosenNumber === 37;