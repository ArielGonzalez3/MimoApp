/** Codificar una sentencia if con la condicion en true.
 * dentro del bloque de codigo codificar una string que se muestre en la consola
 */

if (true) {
  console.log('Hello');
}

/** Crear una vatiable const llamada isRainy y asignarle un valor booleano . Luego, 
 * codificar una sentencia if que use isRainy como condicion. Mostrar algo en consola 
 * si isRainy es true
 */

const isRainy = true;

if (isRainy) {
  console.log('Take an umbrella');
}

/** Crea una variable constante llamada onVacation y asignele el valor true.
 * establezca la variable como condicion de la declaracion if
 */

const onVacation = true;

if (onVacation){
  console.log('Send out-of-ofice email');
}

/** Crear una variable conste llamada cancel. Asignale un valor booleano 
 * de tu eleccion y usalo como condicion para la sentencia if
 */

const cancel = true;

if (cancel) {
  console.log('Are you sure you want to cancel your appointment?')
}

/** Establecer gotMushroom en true. Luego, codificar una sentencia if al rededor 
 * de la declaracion de visualizacion y use gotMushroom como condicion
 */

const gotMushroom = true;

if(gotMushroom) {
  console.log('Use speed boost')
}

/** codifique una sentencia if con true como condicion. Luego dentro del bloque 
 * de codigo escriba una declaracion de visualizacion de string
 */

if(true) {
  console.log('Condicionales')
}

/** Agregar dos o mas console.log 183 no esten vacios al bloque de codigo de la 
 * declaracion if
 */

const doChores = true;

if (doChores) {
  console.log('Hello');
  console.log('World');
}

/** Codificar una sentencia if con la condicion en true alrededor de la 
 * declaracion de visualizascion
 */

if (true) {
  console.log('Jill is online');
}

/** Crear una variable const llamada levelCompleted. Asignale un valor 
 * booleano de tu eleccion. Luego usarlo como condicion de la sentencia if
 */

const levelCompleted = true;

if (levelCompleted) {
  console.log('Ontinue to level 3')
}